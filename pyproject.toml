[project]
name = "streamingetl"
version = "0.1.0"
description = "Step-by-step streaming ETL learning project with Wikimedia."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests-sse",
]
